# Landing preview demo

Static reference mockup (desktop + mobile) for an internal product review. No frameworks, no build step — plain HTML/CSS/JS, viewable directly via GitHub Pages.

An interactive editable version also exists in a separate tool; this repo is the portable, dependency-free copy for sharing outside that environment.
